# Managed by Puppet

user = <%= @db_user %>
password = <%= @db_passwd %>
hosts = <%= @db_host %>
dbname = <%= @db_name %>
dbname = postfix
query = SELECT maildir FROM mailbox WHERE username='%s' AND active = '1'
